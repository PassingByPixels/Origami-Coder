---
name: qwen-shaped-blender
description: Drive a running Blender through the plugin's MCP tools, then verify the result with a viewport screenshot.
---

# Blender

Fixture body. The Qwen plugins keep their skill at `skill/SKILL.md` (singular)
and point at it from the manifest's `skills` array, which is exactly why the
loader cannot assume the standard `skills/<name>/SKILL.md` layout.
